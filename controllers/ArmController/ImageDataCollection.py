
import pandas as pd

df = pd.DataFrame(columns=['filename', 'id', 'center_x', 'center_y', 'w', 'h', 'category', 'relOrientation'])


# import json




# otherDataPath = 'path/to/otherData'

# otherData = dict(model=obj.getModel(), position=list(obj.getPosition()))
# with open(os.path.join(otherDataPath,f'{id}.txt'), 'w+') as f:
#     json.dump(otherData, f, indent=4)
    


# img.save(path/images/id.png)
# coord.save(path/coords/id.txt)
# otherData.save(path/otherData/id.txt)



# object_data = camera.getObjects()


# def newDetection(**kwargs):
#     pd.concat(kwargs, ignore_index=True, inplace=True)
    
    
    

